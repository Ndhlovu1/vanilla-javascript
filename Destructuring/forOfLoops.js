/**
 * 
 *      Object.create() //Used to instantiate prototypes that are not classes
 * 
 * 
 */

const car = {
    engine : true,
    steering : true,
    speed : "slow"
}

console.log(typeof(car))


